# 🎓 校园二手交易小程序

基于微信小程序原生框架与微信云开发的同校二手交易平台。用户选择学校后，只能浏览、搜索和发布本校商品；沟通围绕商品建立一对一会话，并通过数据库实时监听推送新消息。

## 功能

- 访客按学校浏览、微信登录、学校绑定与本地登录态恢复
- 商品图片上传、发布、同校浏览、关键词搜索与状态管理
- 商品详情、商品关联会话、历史消息、实时新消息与未读数
- 个人资料、我的发布和我的聊天

## 技术结构

```text
miniprogram/                 小程序前端（WXML、WXSS、JavaScript）
  app.js                     云环境初始化与登录状态
  pages/                     登录、商品、聊天、个人中心等页面
cloudfunctions/              微信云函数（Node.js）
  login/                     创建或更新当前用户
  getItems/ getItemDetail/   同校商品查询
  publishItem/ getMyItems/   商品发布和本人商品查询
  createConversation/        获取或创建商品会话
  getConversations/          会话列表（含对方公开资料）
  getMessages/ sendMessage/  聊天历史与发送
  markRead/                  标记当前会话已读
  updateItemStatus/          已售、下架或重新上架
  updateProfile/             更新昵称、头像和微信号
```

## 本地导入与部署

### 1. 前置条件

- 已注册小程序并安装[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)。
- 已开通一个微信云开发环境。

### 2. 导入项目与设置云环境

1. 在微信开发者工具中导入本目录，AppID 使用 `project.config.json` 中的值或替换为自己的 AppID。
2. [miniprogram/app.js](miniprogram/app.js) 当前使用环境 `cloud1-d8gctnpc38f1b09ce`；切换部署环境时同步修改此值。
3. 当前 `schoolList` 仅开放“中国人民大学苏州校区”（ID：`ruc_suzhou`）。

### 3. 创建集合

在云开发控制台的数据库中创建以下集合：

| 集合 | 用途 | 核心字段 |
| --- | --- | --- |
| `users` | 用户资料 | `_openid`、`nickName`、`avatarUrl`、`schoolId`、`wechatId` |
| `items` | 商品 | `_openid`、`title`、`price`、`images`、`schoolId`、`status` |
| `conversations` | 商品会话 | `conversationKey`、`participants`、`itemId`、`unreadCount` |
| `messages` | 聊天消息 | `conversationId`、`participants`、`senderOpenid`、`content` |

商品列表支持同校、分类、成色和价格排序。数据量增加前请在 `items` 集合按控制台提示建立组合索引，至少覆盖：

- `schoolId + status + createTime`
- `schoolId + status + category + createTime`
- `schoolId + status + category + condition + createTime`
- 价格排序时，将最后一个排序字段替换为 `price`

`messages.participants` 是实时监听的授权字段。旧的测试消息如果没有此字段，建议在启用安全规则前删除或补齐为对应会话的参与者数组。

### 4. 部署云函数

在开发者工具中逐个右键 `cloudfunctions/` 下的目录，选择“上传并部署：云端安装依赖”。必须包含新加入的 `getMyItems`。

云函数包统一建议使用 `wx-server-sdk@~4.0.2`；部署时由开发者工具安装依赖，不需要在仓库根目录执行 npm install。

### 5. 数据库安全规则

不要把集合设置为“所有用户可读写”。除实时聊天消息外，前端的数据访问都走云函数；云函数会使用服务端权限验证当前 `OPENID`。

在控制台启用以下最小原则：

| 集合 | 客户端权限建议 | 原因 |
| --- | --- | --- |
| `users` | 禁止客户端读写 | 避免泄露 `wechatId`；公开昵称头像由 `getConversations` 和 `getItemDetail` 返回。 |
| `items` | 禁止客户端读写 | 商品浏览、发布和状态更新均由云函数校验。 |
| `conversations` | 禁止客户端读写 | 会话参与关系与未读数只能由云函数维护。 |
| `messages` | 仅允许会话参与者读取；禁止客户端写入 | `chat` 页需要实时 `watch`，发送消息仍只经 `sendMessage`。 |

消息集合的实际规则表达式请使用控制台当前版本支持的“当前用户 OpenID 包含在 `doc.participants` 中”的写法；保存后用两个不同测试账号验证，未参与会话的账号不能读取消息。规则语言会随控制台版本调整，以控制台示例为准。

### 6. 云存储

商品和头像会分别上传到 `items/` 与 `avatars/`。发布前请在云存储安全设置中确认：登录用户可上传；图片可由需要展示它们的登录用户读取。不要上传包含个人敏感信息的图片。

## 手工验收清单

1. 未登录用户进入首页后先选择学校，可上下滑动浏览；点击搜索、分类、商品、发布或个人页时进入登录界面。
2. A 校发布商品后，A 校的其他账号可在首页和搜索中看到；B 校账号无法通过商品 ID 打开详情。
3. 商品详情中买家可创建会话；连续点击或两个请求同时到达时只保留一个会话。
4. 买卖双方可实时收到新消息；进入会话会清除自己的未读数；第三方账号无法读取或标记该会话。
5. 发布者可标记已售、下架和重新上架，其他账号不能修改。
6. “我的发布”、聊天列表和资料修改正常工作，且用户资料不会直接暴露微信号。

## 已知限制

- 学校列表和“已验证”状态目前是示例实现，尚未接入真实校园邮箱或统一认证。
- 商品搜索是标题正则匹配，数据量较大时应改用索引或搜索服务。
- 实时聊天依赖云数据库 `watch`；大量会话场景需要评估配额、监听生命周期和分页策略。

## 开发检查

本仓库没有根目录构建或测试脚本。修改后可至少执行：

```bash
for f in cloudfunctions/*/index.js miniprogram/pages/*/*.js miniprogram/pages/mine/*/*.js miniprogram/app.js; do
  node --check "$f" || exit 1
done
```

之后在微信开发者工具中真机/模拟器编译，并按上方验收清单使用两个测试账号验证云端权限和实时消息。
